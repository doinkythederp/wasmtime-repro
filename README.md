# Shared Memory Debugging Repro

- Open in a Dev Container (e.g. using Codespaces or locally)
- Add a breakpoint on the comment in `example/src/main.rs`
- Use the VS Code debug panel to compile and run
